/*
public class Stat
{
    //fram rate per seconds
    public float fps;
    public float fpsToDisplay;
    public string FormatFpsToDisplay() { return fpsToDisplay.ToString("0.000"); }
    float gcTotalMemory;

    public void Update()
    {
        fpsToDisplay = fps;
        gcTotalMemory = (((float)System.GC.GetTotalMemory(false)) / 1024 / 1024);
    }
}
 */